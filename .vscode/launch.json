{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Django",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/web/manage.py",
            "args": ["runserver"],
            "django": true,
            "cwd": "${workspaceFolder}/web"
        }
    ]
}